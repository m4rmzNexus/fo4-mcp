# OS-02 — Common world base-records: CONT/DOOR/STAT/LIGH/ALCH (+INGR)

**verdict:** confirmed-open | **effort:** L | **risk:** low | **recommend_now:** True

**area:** record-authoring core

## What's missing
Cannot author a new container (loot stashes), door, static/collection (settlement props), light, or consumable (chem/food/stimpak). Only ACTI is an authorable interactable base; everything else must reference existing vanilla bases.

## Evidence (code-grounded)
GAP IS REAL. The authoritative supported-types list is mcp-server/fo4_mcp/tools.py:554 (_CREATE_SUPPORTED_TYPES) = npc, armor, quest, keyword, formlist, message, global, faction, levelednpc, leveleditem, leveleditemoverride, cell, celloverride, smqn, activator, location, locationreftype, encounterzone, package, book, misc, materialswap. NONE of CONT/DOOR/STAT/LIGH/ALCH/INGR appear. The C# writer's type switch (Program.cs:1350 `switch (r.Type?.Trim().ToLowerInvariant())`) has cases through 'activator' (2571), 'location' (2599), 'package' (2645) but NO container/door/static/light/ingestible/ingredient case. Unsupported types are rejected at tools.py:967.

CLAIM IS DIRECTIONALLY CORRECT BUT IMPRECISE: ACTI is not the only authorable interactable base — BOOK (Program.cs:2118) and MISC (2167) are also authorable WITH model+keywords+value+weight+OBND+materialswap. But the six named base types are genuinely all missing.

APPROACH VERIFIED FEASIBLE (reflection over tools/mutagen-cli/Mutagen.Bethesda.Fallout4.dll, v0.53.1): all target types exist — Mutagen.Bethesda.Fallout4.{Ingestible,Container,Door,Static,Light,Effect,EffectData,MagicEffect}. Property shapes confirmed: Ingestible{Name:TranslatedString, Value:UInt32, Weight:Single, Model, Keywords, Flags, Effects:ExtendedList<Effect>}; Container{Name, Model, Items:ExtendedList<ContainerEntry>, Keywords, Flags}; Door{Name, Model, Keywords, Flags}; Static{Name, Model, ObjectBounds}; Light{Name, Model, Radius:UInt32, Color, Value, Weight, Keywords, Flags}; Effect{BaseEffect:IFormLinkNullable<MGEF>, Data:EffectData{Magnitude:Single, Area:Int32, Duration:Int32}}.

NEAR-ZERO NEW SPEC FIELDS NEEDED: RecordSpec (Program.cs:3275) already has Name, Model, MaterialSwap, Keywords, ObjectBounds, Value, Weight, Flags, Items, Inventory(List<ItemSpec>{Item,Count} at 3500). Container contents reuse the EXACT ContainerEntry/ContainerItem idiom already at Program.cs:1451 (NPC inventory). Only NEW fields: Light Radius/Color (or defer color), and an Effects list for ALCH/INGR (new EffectSpec class — the one genuinely new sub-builder). Note Value is currently int? but Ingestible/Light Value is UInt32 — same cast pattern Book already uses at 2134 ((uint) with <0 guard).

ROADMAP: docs/world-content-quest-roadmap.md and V2-backlog.md do NOT track these as a planned item except a passing 'workshop STAT-COBJ' niche note (V2-backlog.md:464) — so this is net-new authoring surface, not a half-finished thread.

Build/test infra is mature: 23 existing create cases each follow identical AddNew→set-fields→read-back-proof pattern; test_tools_create_record.py has ~40 cases mixing pure-validation (tmp_path) + real-writer round-trip (staging/, skip-if-not-built). Rebuild path documented in MANIFEST.md:307: dotnet publish src/Mutagen.RecordQuery.csproj -c Release -r win-x64 --self-contained false -o tools/mutagen-cli.

## Concrete plan

1. C# RunCreate switch (Program.cs ~2571, after the 'activator' case): add 'static' — var st = mod.Statics.AddNew(r.EditorId); set Name if present; Model via the existing `new Model{File=r.Model}` + optional MaterialSwap block (copy from misc:2195); apply OBND from r.ObjectBounds (reuse misc:2210 default-or-spec logic).
2. C# add 'door' case: mod.Doors.AddNew; Name + Model(+MSWP) + Keywords (copy the keyword loop from activator:2580) + Flags via Enum.TryParse<Door.Flag> (copy the faction-flag loop pattern at 2299).
3. C# add 'light' case: mod.Lights.AddNew; Name + Model(+MSWP) + Keywords + Value(uint cast w/ <0 guard, like book:2131) + Weight + Flags<Light.Flag>; map new spec field Radius->light.Radius (uint). Defer Color (System.Drawing surface, same deferral note as keyword:2080) unless trivially set.
4. C# add 'container' case: mod.Containers.AddNew; Name + Model(+MSWP) + Keywords + Flags<Container.Flag>; build Items from r.Inventory (List<ItemSpec>) using the SAME ContainerEntry/ContainerItem idiom already at Program.cs:1451 — `var e=new ContainerEntry{Item=new ContainerItem{Count=it.Count}}; e.Item.Item.SetTo(key); cont.Items.Add(e);`.
5. C# add a new EffectSpec class (near ItemSpec:3500): {string? BaseEffect; float Magnitude; int Area; int Duration;} and a List<EffectSpec>? Effects field on RecordSpec. Add BuildEffect helper (TryKey the BaseEffect MGEF, set Data=new EffectData{Magnitude,Area,Duration}).
6. C# add 'ingestible'(ALCH) and 'ingredient'(INGR) cases: AddNew; Name + Model(+MSWP) + Keywords + Value(uint) + Weight + Flags<Ingestible.Flag>; build Effects via the new helper. INGR mirrors ALCH (Ingredient also has Name/Value/Weight/Model/Effects in Mutagen FO4) — share one branch like book/misc share at 2167.
7. C# read-back proof block (Program.cs ~2937, near the Activators read-back): add foreach over check.Statics/Doors/Lights/Containers/Ingestibles/Ingredients emitting modelFile, keywordCount, name, and per-type counts (itemCount for CONT, effectCount for ALCH/INGR, radius for LIGH) — match the existing dict pattern so tests can assert round-trip.
8. Python: extend _CREATE_SUPPORTED_TYPES at tools.py:554 with 'static','door','light','container','ingestible','ingredient'.
9. Python normalizer: add elif branches in fo4_create_record (model after tools.py:2224 activator block). STAT/DOOR/LIGH/CONT reuse the existing keyword/model/objectBounds/value/weight/flags normalization already written for book/misc (2892) and activator (2224) — factor a shared helper or inline. For CONT, normalize 'contents'/'items' as [{item,count}] -> rec['inventory'] reusing the NPC inventory shape. For ALCH/INGR, normalize 'effects' as [{baseEffect, magnitude, area, duration}] with baseEffect required (raise Fo4McpError if missing, mirroring _norm_conditions function-required guard). For LIGH normalize optional 'radius' (uint, >=0).
10. Rebuild the writer: dotnet publish tools/mutagen-cli/src/Mutagen.RecordQuery.csproj -c Release -r win-x64 --self-contained false -o tools/mutagen-cli (per MANIFEST.md:307); update MANIFEST.md sha256(mutagen-cli.dll) — the apphost .exe sha is unchanged across managed rebuilds (MANIFEST.md:185).
11. Update docs/world-content-quest-roadmap.md with a short entry recording the new base-record authoring surface (consistent with the per-W changelog style there).

## Files to touch

- C:/Modding/fo4-mcp/tools/mutagen-cli/src/Program.cs
- C:/Modding/fo4-mcp/mcp-server/fo4_mcp/tools.py
- C:/Modding/fo4-mcp/mcp-server/tests/test_tools_create_record.py
- C:/Modding/fo4-mcp/tools/MANIFEST.md
- C:/Modding/fo4-mcp/docs/world-content-quest-roadmap.md

## Test strategy
Follow the existing two-tier pattern in mcp-server/tests/test_tools_create_record.py. (1) Pure validation tests against _cfg(tmp_path) that raise before the CLI: bad container item without 'item', ALCH effect missing baseEffect, light radius out-of-range, bad door/light flag — mirror test_create_rejects_* style. (2) Real-writer round-trip tests gated by _skip_if_no_writer + staging_out fixture: author one of each type referencing vanilla masters for FormLink fields (e.g. a STAT with a vanilla MODL path string; a CONT with items=[{item:'<vanilla MISC>:Fallout4.esm',count:5}] asserting itemCount==1 + 'Fallout4.esm' in masters; an ALCH with effects=[{baseEffect:'<vanilla MGEF>:Fallout4.esm',magnitude:25,duration:0}] asserting effectCount==1 + master auto-add; a LIGH asserting radius round-trips). Assert data['wrote'] is True, record FormKeys >=0x800 (ESL-safe), and read-back counts. Add at least one fo4_inspect_record cross-check per new type (record_type == 'Container'/'Static'/etc.) for independent-parse proof, matching test_create_quest_with_dialogue. Optionally one Spriggit-export assertion (skip-if-not-extracted) for ALCH Effects shape, mirroring test_create_faction_record. Run: pytest mcp-server/tests/test_tools_create_record.py.


## Blockers / scope notes
None. Fully autonomous: no GUI, no credentials, no user decision. The only judgment call is whether to defer LIGH Color (System.Drawing.Color surface) — recommend deferring it MVP-style like KYWD Color/Type already is (Program.cs:2080), so it is not a blocker. All target types + properties are reflection-verified present in the installed Mutagen.Bethesda.Fallout4.dll 0.53.1; the build toolchain (net9 dotnet publish) is documented and already used to produce the current binary (dll dated Jun 23).
