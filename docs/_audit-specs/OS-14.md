# OS-14 — NPC Essential/Protected flags + bespoke OTFT outfit + ambush faction tuning

**verdict:** partially-done | **effort:** S | **risk:** low | **recommend_now:** True

**area:** quest + dialogue / NPC

## What's missing
npc.Flags (Essential/Protected) is never set, so a quest-critical NPC (e.g. Kerem) can die before turn-in -> soft-lock. type=outfit (OTFT.AddNew+Items) not implemented. Existing Aggression/Assistance/Sandbox-package fields aren't wired into specs so NPCs fight instead of fleeing.

## Evidence (code-grounded)
Verified against code + reflected the Mutagen.Bethesda.Fallout4 0.53.1 API directly.

CLAIM 1 (npc.Flags Essential/Protected never set) = CONFIRMED-OPEN. The NPC writer case Program.cs:1352-1497 wires Race/Class/Factions/Voice/CombatStyle/DefaultOutfit/AttackRace/Skin/Aggression/Confidence/Assistance/Responsibility/Mood/Keywords/Inventory/Perks/DefaultTemplate/UseTemplateActors/Packages but NEVER touches npc.Flags. The `Flags` field on RecordSpec (Program.cs:3359, documented 'Quest.Flag names') is consumed only by quest(1581)/faction(2305)/levelednpc(2336)/leveleditem(2366)/cellOverride(2506)/smqn(2542)/eczn(2633)/package(2666), NOT npc. Python normalization for npc (tools.py:987-1141) ends at useTemplateActors and never passes a `flags` key. Reflection confirms the fix is API-valid: npc.Flags is type Npc.Flag, enum = Female,Essential,IsCharGenFacePreset,Respawn,AutoCalcStats,Unique,DoesntAffectStealthMeter,CalcForEachTemplate,Protected,Summonable,DoesNotBleed,BleedoutOverride,OppositeGenderAnims,SimpleActor,NoActivationOrHellos,DiffuseAlphaTest,IsGhost,Invulnerable. So dev_approach's `npc.Flags |= Npc.Flag.<name>` is correct (NOT npc.Configuration.Flags).

CLAIM 2 (type=outfit / OTFT.AddNew+Items not implemented) = CONFIRMED-OPEN. 'outfit' is absent from _CREATE_SUPPORTED_TYPES (tools.py:554) and there is no case 'outfit' in Program.cs. Reflection confirms feasibility: Fallout4Mod.Outfits exists (Fallout4Group), Outfit.Items is ExtendedList<IFormLinkGetter<IOutfitTargetGetter>> (target ARMO/LVLI/NPC_), EditorID-constructible like Books (mod.Books.AddNew at 2127).

CLAIM 3 (Aggression/Assistance/Sandbox-package + faction Relations not wired) = REFUTED / ALREADY-DONE. Aggression(1403), Confidence(1409), Assistance(1415), Responsibility(1421), Mood(1427) all wired+validated; npc.Packages binding of existing PACK FormLinks incl. any vanilla Sandbox/Flee package wired at 1493-1495; faction Relations for ambush reactions fully supported via type=faction interfactionRelations (2308-2318, CombatReaction Neutral|Enemy|Ally|Friend) with a PASSING test at test_tools_create_record.py:917. Only the two record-authoring gaps (NPC flags + OTFT) are genuinely missing.

Read-back echo path: Program.cs:2736-2762 builds the per-NPC `back` dict the Python tests assert on (aggression at 2748, useTemplateActors raw-int at 2761); the new flags field must be echoed here (mirror useTemplateActors: `["flags"] = (int)g.Flags`).

## Concrete plan

1. Program.cs RecordSpec: add `public List<string>? NpcFlags { get; set; } // Npc.Flag names (Essential|Protected|Invulnerable|Unique|Respawn|...) OR'd` near the NPC fields (~line 3299). Do NOT reuse the existing `Flags` field — it is the Quest/Faction flag field; overloading it would collide in type-shared specs (matches how UseTemplateActors got its own list).
2. Program.cs case 'npc' (after the Packages block ~1495, before formKey=): add `if (r.NpcFlags is { Count: > 0 }) { foreach (var fn in r.NpcFlags) { if (!Enum.TryParse<Npc.Flag>(fn, true, out var nf)) return Fail($"bad npc flag '{fn}' (Essential|Protected|Invulnerable|Unique|Respawn|Summonable|...)"); npc.Flags |= nf; } }` (mirror faction-flag accumulator 2299-2306).
3. Program.cs read-back: in the NPC `back` dict (2736-2762) add `["flags"] = (int)g.Flags,` so the round-trip test asserts the raw bitfield (mirror useTemplateActors at 2761).
4. Program.cs add `case "outfit":` near book:2127 / activator:2578: `var otft = mod.Outfits.AddNew(r.EditorId); if (r.Items is { Count: > 0 }) foreach (var it in r.Items) { if (!TryKey(it, out var ik, out var e)) return Fail(e); otft.Items.Add(new FormLink<IOutfitTargetGetter>(ik)); } formKey = otft.FormKey.ToString(); break;` — reuse the EXISTING RecordSpec.Items field (line 3314, already used by FormList), no new field needed.
5. Program.cs read-back: add `foreach (var g in check.Outfits) back[g.FormKey.ToString()] = new Dictionary<string,object?>{ ["itemCount"] = g.Items?.Count ?? 0 };` next to the other read-back loops (~2900).
6. tools.py: add 'outfit' to _CREATE_SUPPORTED_TYPES (line 554).
7. tools.py: in npc normalization (after useTemplateActors ~1141) add a `flags`->`npcFlags` passthrough: `nfl = r.get('flags'); if nfl is not None: (list-check) rec['npcFlags'] = [str(x).strip() for x in nfl]` so author specs use the natural `flags` key (Essential/Protected) while avoiding the C# Flags-field collision.
8. tools.py: add an `elif rtype.lower() == 'outfit':` normalization branch validating `items` is a list of non-empty FormKey strings -> rec['items'] (mirror the FormList items validation already in the file).
9. tools.py: extend docstring/type lists at 810/845/971-972 and the tool description (~2432-2464) to mention npc `flags` (Essential/Protected) and the new `outfit` type.
10. Rebuild the CLI: dotnet build tools/mutagen-cli/src -c Release (Python subprocess-calls the freshly built mutagen-cli.dll; tests _skip_if_no_writer when not built).
11. Author-spec wiring for the live Kerem chain (spec/doc only, no code): set the quest-giver NPC `flags:["Essential"]` (or Protected) so he cannot die pre-turn-in, closing the soft-lock; optionally author a bespoke OTFT and point the NPC's existing defaultOutfit at it.

## Files to touch

- C:/Modding/fo4-mcp/tools/mutagen-cli/src/Program.cs
- C:/Modding/fo4-mcp/mcp-server/fo4_mcp/tools.py
- C:/Modding/fo4-mcp/mcp-server/tests/test_tools_create_record.py

## Test strategy
Add to mcp-server/tests/test_tools_create_record.py (matches existing real_env/staging_out/_skip_if_no_writer pattern). 1) test_create_npc_with_flags: spec npc flags:["Essential","Protected"]; call fo4_create_record; assert data["wrote"]; assert read-back rec["flags"] has Essential(2)|Protected(256)=258 bits set; fo4_inspect_record round-trips (Spriggit 2nd engine, like W3b/W3c tests). 2) test_create_rejects_npc_bad_flag: flags:["Nope"] -> expect failure (mirror test_create_rejects_npc_bad_aggression:973). 3) test_create_outfit_roundtrip: type Outfit editorId+items:["<armor FormKey>:Fallout4.esm"]; assert wrote + read-back itemCount==1 + Fallout4.esm in masters + inspect finds record_type=="Outfit". 4) test_create_rejects_outfit_non_list_items: items:"nope" -> reject (mirror test_create_rejects_npc_non_list_inventory:953). Run: pytest mcp-server/tests/test_tools_create_record.py -q. All output stays in staging/ (staging_out fixture); no Steam/Data writes; gated fo4_create_record enforces the path boundary via scripts/precheck_path.py.


## Blockers / scope notes
None. Fully autonomous (no GUI/credential/user decision). Mutagen API reflection-verified (npc.Flags:Npc.Flag with Essential/Protected; mod.Outfits + Outfit.Items:ExtendedList<IFormLinkGetter<IOutfitTargetGetter>>). Output goes to staging/ via existing gated fo4_create_record. ONE scoping correction to the synthesis: claim 3 (Aggression/Assistance/Sandbox-package + faction Relations) is ALREADY implemented and tested — do not re-add it; the real work is only (a) the npc flags bitfield and (b) the new type=outfit case. Author-spec edits to set Essential on Kerem are a separate spec/doc step (showcase wiring), not a code change.
