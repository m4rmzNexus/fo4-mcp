# OS-11 — Widen field coverage on glue records (MESG/FACT/LVLI/LVLN: buttons, ranks, chanceNone)

**verdict:** partially-done | **effort:** M | **risk:** low | **recommend_now:** True

**area:** record-authoring core

## What's missing
Glue records author only editorId/name + the one-or-two fields the demos needed: MESG has no menu buttons (choice dialogs), FACT no ranks/vendor data (merchants), LVLI/LVLN no chanceNone/filter keywords (loot tuning). Records are structurally valid but functionally minimal. The LVLI chanceNone/UseAll gap is also what blocks coupon loot 'as-extra not-replace'.

## Evidence (code-grounded)
The gap is REAL and explicitly documented as deferred in the writer's own comments. tools/mutagen-cli/src/Program.cs:
- MESG (case "message", 2106-2117): authors only Description (body) + Name (title). Line 2111: "MenuButtons/Conditions/OwnerQuest/Flags/DisplayTime deferred."
- FACT (case "faction", 2290-2322): authors only Name + Flags + Relations. Lines 2294-2296: "Ranks/CrimeValues/VendorValues deferred (richer sub-structures, not needed for the hostility goal)."
- LVLN (case "levelednpc", 2323-2352) / LVLI (case "leveleditem", 2353-2382): author only entries+flags. Line 2328: "chanceNone/Global/MaxCount/FilterKeywordChances deferred." Line 2357: "EpicLootChance/OverrideName deferred."
Python layer mirrors this: mcp-server/fo4_mcp/tools.py:2042 "chanceNone/Global/MaxCount/FilterKeywordChances deferred (§2 — refinements)." Message normalization (1885-1891) only passes text/title. Faction (2004-2038) only flags/relations.
Mutagen API CONFIRMED present + writable (reflected from tools/mutagen-cli/Mutagen.Bethesda.Fallout4.dll v0.53.1): Message.MenuButtons (ExtendedList<MessageButton>, writable; MessageButton has Text:TranslatedString + Conditions:ExtendedList). Faction.Ranks (ExtendedList<Rank>; Rank.Number:byte?, Rank.Title:IGenderedItem<TranslatedString> [gendered Male/Female], Rank.Insignia:string). Faction.VendorValues (IVendorValues, writable: StartHour/EndHour/Radius UInt16 + BuysStolenItems/BuySellEverythingNotInList/BuysNonStolenItems bools). LeveledItem.ChanceNone + LeveledNpc.ChanceNone (Noggog.Percent, writable, 0.0-1.0). LeveledItem.EpicLootChance (IFormLinkNullable<IGlobal>). LeveledItem.MaxCount / Global also present.
CLAIM CORRECTION (already-done, refute the structure here): UseAll + CalculateForEachItemInCount are ALREADY supported and tested. Program.cs:2364-2365 parses LeveledItem.Flag including UseAll; test_tools_create_record.py:1164,1179 asserts flags==4 (UseAll bit). So the structure's assertion that "the LVLI UseAll gap blocks coupon loot as-extra not-replace" is FALSE — UseAll ships. The genuinely missing loot-tuning field is ChanceNone (chance the whole list yields nothing), not UseAll.
Readback path that must be extended in lockstep (Program.cs check-block, the round-trip proof the tests assert on): Factions back-dict at 2890-2896 (name/flagCount/relationCount), LeveledNpcs 2899-2910 + LeveledItems 2911-2922 (entryCount/flags/entries), and Messages currently have NO back-dict entry at all (grep of the check loop shows globals/factions/leveled/SMQN/activator/location/eczn but no check.Messages) — so MESG round-trip assertions need a new foreach.
Test scaffolding exists: real_env + staging_out + _skip_if_no_writer fixtures, Spriggit second-engine cross-check pattern (test ~line 916-948), and existing per-record round-trip tests at 1149-1183 (leveled) and 916-948 (faction).

## Concrete plan

1. Scope decision (keep MVP, ship the high-leverage fields only): MESG.MenuButtons (Text + optional Conditions via the existing _norm_conditions/condition builder); FACT.Ranks (Number + Title.Male/Female + Insignia) and FACT.VendorValues (the 3 bool flags + hours + radius); LVLI/LVLN.ChanceNone (Percent). DEFER FACT.CrimeValues, LVLI.EpicLootChance/MaxCount/Global, MESG.OwnerQuest/DisplayTime — note them as a follow-up line, do not build speculatively (CLAUDE.md rule 2).
2. Program.cs MESG (case 'message', ~2112): after Description/Name, add `if (r.MenuButtons is {Count:>0})` loop -> `foreach` build `var btn = new MessageButton { Text = b.Text };` then if b.Conditions present reuse the existing condition-builder used by quests/packages/SMQN (the writer already has a BuildConditions helper for ConditionSpec — locate it via the SMQN/objective condition path and call it into btn.Conditions); `mesg.MenuButtons.Add(btn)`. MESG normally needs Flags MessageBox bit to show buttons — also wire r.Flags -> mesg.Flags via Enum.TryParse<Message.MessageFlag> (mirror the faction-flag loop at 2299-2307).
3. Program.cs FACT (case 'faction', ~2308 after Relations): add `if (r.Ranks is {Count:>0})` -> `foreach` build `var rk = new Rank { Number = (byte?)rs.Number, Insignia = rs.Insignia };` set gendered title `rk.Title = new GenderedItem<TranslatedString>(rs.TitleMale ?? rs.Title, rs.TitleFemale ?? rs.Title);` then `fact.Ranks.Add(rk);`. Add `if (r.VendorValues is {} vv)` -> `fact.VendorValues = new VendorValues { StartHour = (ushort)vv.StartHour, EndHour=(ushort)vv.EndHour, Radius=(ushort)vv.Radius, BuysStolenItems=vv.BuysStolen, BuysNonStolenItems=vv.BuysNonStolen, BuySellEverythingNotInList=vv.BuyEverything };` (verify the concrete ctor 'VendorValues' name resolves; reflection showed IVendorValues — the concrete is VendorValues).
4. Program.cs LVLN+LVLI (cases ~2329 and ~2359, before entries loop): add `if (r.ChanceNone is {} cn) { if (cn<0||cn>100) return Fail(...); lvln.ChanceNone = new Noggog.Percent(cn/100.0); }` (author as 0-100 integer percent at the spec boundary, store as Percent fraction — matches modder mental model). Same line for lvli.
5. Program.cs RecordSpec class (~3275-3369): add nullable fields `public List<MessageButtonSpec>? MenuButtons; public List<RankSpec>? Ranks; public VendorValuesSpec? VendorValues; public int? ChanceNone;` and define `class MessageButtonSpec { Text; List<ConditionSpec>? Conditions; }`, `class RankSpec { int? Number; string? Title; string? TitleFemale; string? Insignia; }`, `class VendorValuesSpec { int StartHour; int EndHour; int Radius; bool BuysStolen; bool BuysNonStolen; bool BuyEverything; }` near the other *Spec classes (~3486+).
6. Program.cs readback check-block: (a) ADD a new `foreach (var g in check.Messages)` block writing {name, description, buttonCount = g.MenuButtons.Count, buttons:[{text}]} — there is currently NO Messages entry in the back-dict, so MESG round-trip is unprovable today; (b) EXTEND Factions back-dict (2890-2896) with rankCount = g.Ranks.Count and hasVendorValues = g.VendorValues != null; (c) EXTEND LeveledItems(2911) + LeveledNpcs(2899) back-dicts with chanceNone = (int)Math.Round(g.ChanceNone.Value*100).
7. tools.py message normalizer (1885-1891): add menuButtons validation — list of {text:str required, conditions?:reuse _norm_conditions} -> rec['menuButtons']; pass-through flags list (reuse the generic flags handling). Faction normalizer (2004-2038): add ranks validation (list of {number 0-255, title?, titleFemale?, insignia?}) -> rec['ranks']; vendorValues validation (dict, hours 0-65535, radius, 3 bools) -> rec['vendorValues']. Leveled normalizer (2043-2087): add chanceNone int 0-100 range-check -> rec['chanceNone'] for the levelednpc/leveleditem/leveleditemoverride branch.
8. Update the docstring JSON schema in tools.py (~810-934) to document the new optional keys (menuButtons/ranks/vendorValues/chanceNone) under the respective record types, matching the existing inline-comment style.
9. Rebuild the CLI: dotnet publish tools/mutagen-cli/src -c Release (per the apphost-vs-dll + publish!=build gotcha in MEMORY); confirm the staging round-trip still passes the existing 2 leveled + 1 faction tests unchanged.
10. Add pytest cases (see test_strategy). Run the create-record suite; update TASKS.md retrospektif + the deferred-field comments in Program.cs/tools.py to point at what is now done vs still deferred.

## Files to touch

- C:/Modding/fo4-mcp/tools/mutagen-cli/src/Program.cs
- C:/Modding/fo4-mcp/mcp-server/fo4_mcp/tools.py
- C:/Modding/fo4-mcp/mcp-server/tests/test_tools_create_record.py
- C:/Modding/fo4-mcp/TASKS.md

## Test strategy
Extend mcp-server/tests/test_tools_create_record.py following the existing real_env/staging_out/_skip_if_no_writer + Spriggit-cross-check pattern. Gating tests (no binary needed, raise-before-CLI): bad chanceNone out of range (e.g. 150) on LeveledItem; menuButtons not a list / button missing text; faction ranks number out of range; vendorValues bad hour. Round-trip tests (drive real writer, assert on the extended back-dict): (1) Message with 2 MenuButtons + MessageBox flag -> assert rec buttonCount==2 and buttons[0].text; cross-check Spriggit YAML contains 'MenuButtons:' and the button text. (2) Faction with 2 Ranks (Number+Title) + VendorValues(BuysStolenItems=true) -> assert rankCount==2, hasVendorValues true; Spriggit YAML contains 'Ranks:' and 'VendorValues:'. (3) LeveledItem + LeveledNpc with chanceNone=25 -> assert rec chanceNone==25 round-trips; reuse the existing Caps/Codsworth fixtures. Reuse vanilla Fallout4.esm FormKeys already used in the file (068043 MinutemenFaction, 00000F Caps) to keep master auto-add deterministic. Full suite must stay green (claimed 402 tests); the existing leveled/faction tests must pass UNCHANGED (additive fields only).


## Blockers / scope notes
None for autonomous build. Two design notes, not blockers: (1) Rank.Title is gendered (IGenderedItem<TranslatedString> Male/Female) — the plan collapses a single 'title' into both genders with optional 'titleFemale' override, which is the correct MVP and avoids a user decision. (2) The structure's premise that 'UseAll gap blocks coupon loot as-extra not-replace' is factually wrong — UseAll already ships (Program.cs:2364, tested at test:1179); the real coupon-loot lever is ChanceNone, which IS in scope here. Recommend correcting that line in the backlog so the coupon-showcase task isn't mis-scoped. No GUI/credential/Steam-write involved; output stays in staging/ per the safe-write boundary.
