# OS-05 — INFO SNAM round-trip test + shared conftest.py fixtures

**verdict:** partially-done | **effort:** S | **risk:** low | **recommend_now:** True

**area:** quest + dialogue / test infra

## What's missing
The shipped SNAM (setParentQuestStage) writer path has Python validation (tools.py:1438-1465) but zero positive round-trip assertion, so the documented P0 acceptance is unverified in CI. Separately, test fixtures are copied across 21 files with no conftest.py.

## Evidence (code-grounded)
SNAM writer path is fully implemented but has ZERO round-trip test, and no conftest.py exists. (1) Python validation: mcp-server/fo4_mcp/tools.py:1438-1465 normalizes resp.setParentQuestStage{onBegin,onEnd} (range -1..32767). (2) C# writer: tools/mutagen-cli/src/Program.cs:1766-1772 sets info.SetParentQuestStage = new DialogSetParentQuestStage{OnBegin,OnEnd}. (3) Readback ALREADY EXISTS via the dialogue-dump subcommand: Program.cs:46 dispatches "dialogue-dump"; Program.cs:535-536 surfaces setStageOnBegin/setStageOnEnd per INFO (P3 was implemented — docs:344 said it was a gap, but the code now emits it). (4) GAP #1: grep for setParentQuestStage|setStageOnEnd|stageOnEnd across mcp-server/tests/ returns EMPTY — no test asserts the round-trip. docs/fo4-quest-dialogue-system.md:372-374 defines the P0 acceptance verbatim ("author an INFO with setParentQuestStage.onEnd=20, reopen, assert OnEnd==20; SNAM survives round-trip; dialogue-dump reports it") — so the documented P0 acceptance is unverified in CI. (5) The create-record quest readback (Program.cs:2809-2839) does NOT include any SNAM key, so fo4_create_record's response alone cannot prove the round-trip — the test MUST go through dialogue-dump. (6) There is NO Python wrapper for dialogue-dump (grep dialogue_dump in mcp-server/ = empty); fo4_inspect_sm_tree (tools.py:2951-3000) is the exact template for adding one. (7) GAP #2 (conftest): no conftest.py anywhere; BUT the proposal's "37 redefinitions across 21 files" is WRONG — real_env is defined in 6 files (create_record, inspect_record, lint_npc_template, spriggit, w12_handoff, w9_w10_bake), staging_out in 4 files, 6 files total. (8) Binary IS built (tools/mutagen-cli/mutagen-cli.exe present) so an integration test will RUN, not skip. _mutagen_cli_binary at tools.py:3683; run_tool at subprocess_wrap.py:37.

## Concrete plan

1. Step 1 (the high-value half): In mcp-server/tests/test_tools_create_record.py add test_create_quest_info_set_parent_quest_stage(real_env, staging_out). Build a quest with one topic whose response carries setParentQuestStage:{onEnd:20} plus a SECOND control topic with NO setParentQuestStage. Call fo4_create_record to write the esp (gated output under staging/, which the existing staging_out fixture already provides and cleans up).
2. Step 2: Resolve the CLI via _mutagen_cli_binary(cfg, manifest) (skip via _skip_if_no_writer like every other integration test), then invoke the read-only dialogue-dump subcommand directly with run_tool(cli, ['dialogue-dump','--plugin',str(out),'--quest','<EditorID>'], env_extra=_spriggit_env()). Import run_tool and _mutagen_cli_binary/_spriggit_env at top of the test file (mirror the existing imports from fo4_mcp.tools).
3. Step 3: json.loads(result.stdout); locate the SNAM-bearing INFO and assert its setStageOnEnd == 20 (Program.cs:536), and setStageOnBegin == -1 (the null->-1 default, Program.cs:1771). For the control topic's INFO assert setStageOnEnd == -1 (no-SNAM => DialogSetParentQuestStage never written => the field defaults to -1; confirm the dump emits -1 vs null and assert accordingly — read Program.cs:535-536: info.SetParentQuestStage?.OnEnd is null when the subrecord is absent, so assert it is null/None for the control, NOT -1).
4. Step 4 (optional but cheap): add a no-CLI Python-validation test test_create_rejects_set_parent_quest_stage_out_of_range(tmp_path) asserting setParentQuestStage:{onEnd:99999} raises Fo4McpError (exercises tools.py:1457-1462), matching the existing pure-validation test style — runs without the binary.
5. Step 5 (the lower-value conftest half — do as a SEPARATE commit, scoped to reality): create mcp-server/tests/conftest.py defining the canonical real_env and staging_out fixtures (copy from test_tools_create_record.py:270-281, parametrize staging_out's dir name or keep per-test dirs). Then DELETE the duplicate fixture defs from the 6 files that redefine them (create_record, inspect_record, lint_npc_template, spriggit, w12_handoff, w9_w10_bake). Note staging_out dirs differ per file (faz1-create-test etc.) — either make conftest's staging_out yield a unique tmp under staging/ or keep file-local staging_out where the dir name is load-bearing. Run full suite to confirm 393 PASS unchanged.
6. Step 6: run pytest mcp-server/tests/test_tools_create_record.py -k set_parent_quest_stage to confirm the new test passes against the built binary, then the full suite.

## Files to touch

- mcp-server/tests/test_tools_create_record.py
- mcp-server/tests/conftest.py
- mcp-server/tests/test_tools_inspect_record.py
- mcp-server/tests/test_tools_lint_npc_template.py
- mcp-server/tests/test_tools_spriggit.py
- mcp-server/tests/test_tools_w12_handoff.py
- mcp-server/tests/test_tools_w9_w10_bake.py

## Test strategy
Fits the existing pytest suite exactly: the SNAM test is a real_env/staging_out integration test guarded by _skip_if_no_writer (skips when binary absent, runs in CI here since mutagen-cli.exe is built). Output is written only under staging/ (gated, staging_out fixture rmtree-cleans it) — never the Steam Data folder, so the hard write-boundary rule is honored and no diff/approval flow is needed (test-only, autonomous). Round-trip is proven by an INDEPENDENT read path (dialogue-dump subprocess re-reads the on-disk binary), matching the project's existing round-trip-proof contract (cf. Spriggit-based proofs at test_tools_create_record.py:753-789). The positive assertion (setStageOnEnd==20) plus the no-SNAM control (assert None) gives the P0 acceptance from docs/fo4-quest-dialogue-system.md:372-374. The pure-validation test needs no binary. The conftest dedup is verified by running the full suite (currently ~393 PASS per CLAUDE.md) and confirming the count is unchanged.


## Blockers / scope notes
None. Autonomous (test-only + test-infra), no user decision/GUI/credential. Writer + dialogue-dump readback already shipped; only the assertion is missing. Note: do the SNAM test FIRST (high leverage, closes the documented-but-unverified P0 acceptance) and treat the conftest dedup as a smaller, separable second commit — the proposal overstated its size ("37 redefinitions / 21 files" is actually 6 files; real_env x6, staging_out x4), so it is lower-leverage than billed but still a valid cleanup. Watch one detail: a no-SNAM INFO yields info.SetParentQuestStage == null, so the control assertion must check for None, not -1 (-1 is only the default when the subrecord IS written with an omitted onBegin/onEnd).
